<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Send absolute URLs to index.html for SPA routing
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.html [L]
</IfModule>
